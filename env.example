# Myrient ROM Downloader Configuration
# Copy this file to .env and modify as needed

# Base URL to download from
BASE_URL=https://myrient.erista.me/files/FULL/PATH/TO/PAGE/HERE

# Directory to save downloaded files
DOWNLOAD_DIR=./NameOfConsole_here

# Number of concurrent download threads (Myrient requires 1 connection per file;
# keep this low to avoid triggering abuse detection / 10 KB/s throttle)
MAX_THREADS=3

# Request timeout in seconds
TIMEOUT=30

# SQLite database file path
DB_FILE=downloads.db

# User agent string for HTTP requests (Wget is explicitly supported by Myrient)
USER_AGENT=Wget/1.21.3

# Region filter (comma-separated, e.g. USA,Europe,Japan)
# Aliases: EU=Europe, JP=Japan, AUS=Australia, etc.
# REGION=USA

